# Note only, not used
repo_id: YOUR_HF_USER/my_so101_dataset
root: data
push_to_hub: false
num_episodes: 50
single_task: grab the block
fps: 30
episode_time_s: 20
reset_time_s: 10
streaming_encoding: true
encoder_threads: 2
